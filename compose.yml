services:
  streamlit:
    build: .
    command: ["streamlit", "run", "app.py"]
    env_file: .env
    ports:
      - "3051:8501"
    volumes:
      - shared_data:/app/shared_data

volumes:
  shared_data:

